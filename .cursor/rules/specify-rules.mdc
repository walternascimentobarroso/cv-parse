---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# CV Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-11

## Active Technologies
- Python 3.12 (existing) + FastAPI, motor, pdfplumber, pydantic-settings (existing); pytest, httpx already in pyproject.toml (003-testing-strategy)
- Python 3.12 + FastAPI, motor, pdfplumber, pydantic-settings (004-cv-api-refactor)
- MongoDB (motor); collection and schema unchanged (004-cv-api-refactor)
- Python 3.12 + FastAPI, Motor (async MongoDB), Pydantic (005-extractions-crud-audit)
- MongoDB (Motor async driver) (005-extractions-crud-audit)
- Python 3.12 + FastAPI, Motor (async MongoDB driver), Pydantic, pdfplumber, pydantic-settings (006-structured-cv-parsing)
- MongoDB (existing CV collection; extended document shape with `parsed_data`) (006-structured-cv-parsing)

- Python 3.12 (existing; from 001-doc-to-text-api) + Unchanged from 001 (FastAPI, motor, pdfplumber, pydantic-settings, etc.); managed via uv and `pyproject.toml` (002-dev-env-tooling)

## Project Structure

```text
src/
├── api/
├── domain/
└── infra/
tests/           # Mirror src: tests/domain, tests/api, tests/infra
├── conftest.py
├── domain/
├── api/
└── infra/
```

## Commands

- Install: `make install` or `uv sync`
- Full tests: `uv run pytest tests/` or `make test`
- By layer: `uv run pytest tests/domain/`, `uv run pytest tests/api/`, `uv run pytest tests/infra/`
- Lint: `uv run ruff check .` or `make lint`

## Code Style

Python 3.12 (existing; from 001-doc-to-text-api): Follow standard conventions

## Recent Changes
- 006-structured-cv-parsing: Added Python 3.12 + FastAPI, Motor (async MongoDB driver), Pydantic, pdfplumber, pydantic-settings
- 005-extractions-crud-audit: Added Python 3.12 + FastAPI, Motor (async MongoDB), Pydantic
- 004-cv-api-refactor: Added Python 3.12 + FastAPI, motor, pdfplumber, pydantic-settings


<!-- MANUAL ADDITIONS START -->

## Assert Statements (Bandit)

Do **not** use `assert` for enforcing interface constraints or critical checks. `assert` is stripped when Python runs with optimization (`-O`), so those checks disappear in optimized builds.

- **Use**: explicit checks with `raise AssertionError(...)` so behavior is unchanged with or without `-O`.
- **Good**: `if not condition: raise AssertionError("User must be logged in")` then continue.
- **Bad**: `assert condition` then continue.

Apply this in both production and test code (e.g. in `tests/`).

<!-- MANUAL ADDITIONS END -->
