# CV test fixtures

- **`profile.pdf`** — anonymized sample CV. Regenerate with:
  `uv run python tests/fixtures/build_profile_fixture.py`
- **`profile_expected.json`** — acceptance criteria for integration test (subset checks).
